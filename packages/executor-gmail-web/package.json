{
  "name": "@approval-gated-actions/executor-gmail-web",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "login": "tsx src/cli.ts login",
    "run:once": "tsx src/cli.ts once",
    "run:poll": "tsx src/cli.ts poll",
    "test": "pnpm --dir ../.. exec vitest run --config vitest.config.ts packages/executor-gmail-web/**/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@approval-gated-actions/core": "workspace:*",
    "playwright": "^1.55.1",
    "zod": "^4.1.5"
  }
}
